name: polysome-stats
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - matplotlib
  - seaborn
  - scipy
  - numpy
  - jupyter
  - conda-forge::mrcfile
  - r::r-irkernel
  - pip:
      - starfile
